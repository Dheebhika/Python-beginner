array=[]

ranges=int(input("Enter the range"))
def add_list():
    for i in range(0,ranges):
        number = int(input("Enter  numbers:"))

        array.append(ranges)
    return array
print(add_list())